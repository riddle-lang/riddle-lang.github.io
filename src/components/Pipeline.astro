---
import SectionHead from './SectionHead.astro';
import { inline } from '../lib/inline';
import type { SiteContent } from '../i18n';

interface Props {
  t: SiteContent;
}

const { t } = Astro.props;
---

<section id="pipeline" class="section border-t border-line bg-surface/45">
  <div class="shell">
    <SectionHead
      eyebrow={t.pipeline.eyebrow}
      title={t.pipeline.title}
      subtitle={t.pipeline.subtitle}
    />

    <ol class="mt-14 grid gap-px overflow-hidden rounded-[var(--radius-card)] border border-line bg-line sm:grid-cols-2 lg:grid-cols-3">
      {
        t.pipeline.stages.map((stage, index) => (
          <li
            class="reveal group relative bg-bg p-6 transition-colors duration-300 hover:bg-surface"
            style={`transition-delay:${Math.min(index, 8) * 45}ms`}
          >
            <span class="font-mono text-[13px] tabular-nums text-brand">
              {String(index + 1).padStart(2, '0')}
            </span>
            <h3 class="mt-3 text-[16px] font-medium tracking-tight">{stage.name}</h3>
            <p class="mt-2 text-[13.5px] leading-relaxed text-muted" set:html={inline(stage.desc)} />
            <span
              class="absolute inset-x-0 bottom-0 h-px scale-x-0 bg-brand transition-transform duration-400 ease-[cubic-bezier(.16,1,.3,1)] group-hover:scale-x-100"
              aria-hidden="true"
            />
          </li>
        ))
      }
    </ol>

    <p
      class="reveal mt-6 text-[13.5px] leading-relaxed text-muted"
      set:html={inline(t.pipeline.footnote)}
    />
  </div>
</section>
